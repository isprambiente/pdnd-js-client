{
  "name": "pdnd-js-client",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p .",
    "clean": "rm -rf dist",
    "lint": "eslint . --ext .ts",
    "test": "vitest",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^1.6.1",
    "eslint": "^8.57.1",
    "@types/bun": "latest"
  },
  "dependencies": {
    "jose": "^5.10.0",
    "commander": "^10.0.1",
    "undici": "^6.22.0"
  }
}
